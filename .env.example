# Supabase — Project Settings → API
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Service role key — local seed script only (scripts/seed-admin.mjs); never expose in the app
SUPABASE_SERVICE_ROLE_KEY=
